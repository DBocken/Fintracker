{
  "name": "trackerverse",
  "private": true,
  "version": "0.0.0",
  "description": "Trackerverse monorepo — a suite of local-first tracker apps sharing data through a capability-brokered bridge.",
  "type": "module",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
